name: emergency_cases_saver
description: EC Saver - Emergency Cases Saver for Rescue 1122 Pakistan

version: 1.3.5+35  # v1.3.5 - Fixed keystore path resolution for proper APK signing

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Core dependencies
  cupertino_icons: ^1.0.8
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  intl: ^0.19.0
  
  # UI & Fonts
  google_fonts: ^6.1.0
  
  # Utilities
  url_launcher: ^6.2.2
  provider: ^6.1.1
  
  # PDF & Printing
  pdf: ^3.10.4
  printing: ^5.11.0
  
  # Supabase
  supabase_flutter: ^2.10.3
  
  # Update system (SIMPLIFIED)
  package_info_plus: ^4.2.0
  http: ^1.1.0
  permission_handler: ^11.4.0
  open_file: ^3.3.2
  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/image/logo.png
    - assets/image/background.png
    - assets/image/landpagebackground.jpg
